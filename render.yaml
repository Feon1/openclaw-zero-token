services:
  - type: web
    name: openclaw
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "8080"
      - key: SETUP_PASSWORD
        sync: false
      - key: OPENCLAW_STATE_DIR
        value: /tmp/.openclaw   # изменено с /data
      - key: OPENCLAW_WORKSPACE_DIR
        value: /tmp/workspace   # изменено с /data
      - key: OPENCLAW_GATEWAY_TOKEN
        generateValue: true
    # Секция disk полностью удалена
